decl
    int num,n,c;
    str msg;
enddecl
begin
    read(num);
    read(n);
    read(c);
    msg = "Largest no is";
    write(msg);
    if (num < n) then
        if (n < c) then write(c); else write(n); endif;
    else
        if (num < c) then write(c); else write(num); endif;
    endif;
end