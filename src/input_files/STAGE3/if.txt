begin
    read(a);
    read(b);
    read(c);
    if (a < b) then
        if (b < c) then write(c); else write(b); endif;
    else
        if (a < c) then write(c); else write(a); endif;
    endif;
    i = 10;
    while(i!=12)
    do
        write(i);
        i = i + 1;
    endwhile;
    write(i);
end