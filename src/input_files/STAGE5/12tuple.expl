decl
    tuple student (str name, int roll_no, str branchname, int year_of_admission) a, b, c;
enddecl

int main()
{
    decl
        tuple faculty (str name, int employee_id, str dept) x, y, z, *ftr, l;
        int ans;
    enddecl

    begin
        ftr = &x;
        breakpoint;
        x.name = "hiran";
        write((*ftr).name);
    end
}

