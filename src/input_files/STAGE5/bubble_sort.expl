decl
  int n,i,j,dup, BubbleSort(int first, int last);
enddecl

int BubbleSort(int first, int last)
{
  decl
    int output;
    str a,b;
    int temp;
  enddecl
  begin
    temp = first+last;
    return a;
  end
}

int main()
{
  decl
    int first, r;
  enddecl

  begin
    read(n);

    r = BubbleSort(n,n+1);
    write(r);
  end
}
